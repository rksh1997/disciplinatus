import { library } from '@fortawesome/fontawesome-svg-core';
import {
  faCheckCircle,
  faTimesCircle,
} from '@fortawesome/free-regular-svg-icons';

const icons = [faCheckCircle, faTimesCircle];

library.add(...icons);
